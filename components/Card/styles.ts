import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  cardContainer: {
    width: 30,
    height: 50,
    borderRadius: 4,
  },
  emptyCardSlot: {
    backgroundColor: "#00000029",
  },
  activeCard: {
    backgroundColor: "#FFF",
    padding: 4,
  },
})
