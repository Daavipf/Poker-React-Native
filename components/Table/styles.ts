import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  communityCardsContainer: {
    flexDirection: "row",
    justifyContent: "center",
    gap: 12,
  },
  table: {
    width: "80%",
    height: "80%",
    borderRadius: 9999,
    backgroundColor: "#bfd7bbff",
    borderWidth: 8,
    borderColor: "rgba(120, 156, 120, 1)",
    justifyContent: "center",
    alignItems: "center",
    gap: 16,
  },
})
